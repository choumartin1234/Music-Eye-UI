/**
 * AudioPlayer
 *
 * Usage:
 * ```js
 * const player = new AudioPlayer();
 * player.start(); // start
 * player.pause(); // pause
 * player.reset(); // reset
 * player.moveTo(2); // move to unit 2
 * player.attach(data); // attach data to the end
 * player.replace(2, data); // replace with data from unit 2
 * ```
 */
export class AudioPlayer {
  async start() {
    // TODO
  }

  async pause() {
    // TODO
  }

  async reset() {
    // TODO
  }

  async moveTo(p) {
    // TODO
  }

  async replace(p, data) {
    // TODO
  }

  async attach(data) {
    // TODO
  }
}
